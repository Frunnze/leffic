#!/bin/sh
set -e
. "$(dirname "$0")/_env.sh"

require_binary "$ruff_binary" ruff

echo "pre-commit: ruff (all)"

for service in $services; do
    (cd "$service" && "$ruff_binary" check src run.py)
done
